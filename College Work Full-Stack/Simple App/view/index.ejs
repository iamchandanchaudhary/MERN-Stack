<!DOCTYPE html>
<html>
<head>
    <title>Simple Node App</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
</head>
<body class="bg-light">

<div class="container mt-5">
    <h1 class="text-center mb-4">Simple Node + Bootstrap + MongoDB App</h1>

    <!-- Add Item Form -->
    <form action="/add" method="POST" class="d-flex mb-3">
        <input type="text" name="itemName" class="form-control" placeholder="Enter item" required>
        <button class="btn btn-primary ms-2">Add</button>
    </form>

    <!-- Display Items -->
    <ul class="list-group">
        <% items.forEach(item => { %>
            <li class="list-group-item"><%= item.name %></li>
        <% }) %>
    </ul>

</div>

</body>
</html>
